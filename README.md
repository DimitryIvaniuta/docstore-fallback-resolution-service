# docstore-fallback-resolution-service
Java reference implementation of a safe Docstore consumer-side fallback strategy: resolve documents by canonical product key first, then legacy composite key, with tests and detailed Javadoc.
